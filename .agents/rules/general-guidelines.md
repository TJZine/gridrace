---
trigger: always_on
---

# General Rules

Read `AGENTS.md` first, then the single active plan and the relevant section of
`docs/ENGINEERING_RUNBOOK.md`. Those repository authorities take precedence over
this thin tool-specific shim.
